services:
  - type: web
    name: ecoblock-api
    runtime: node
    plan: free
    rootDir: server
    buildCommand: npm install --legacy-peer-deps
    startCommand: node app.js
    healthCheckPath: /api/health
    envVars:
      - key: NODE_VERSION
        value: 18
      - key: JWT_SECRET
        generateValue: true
